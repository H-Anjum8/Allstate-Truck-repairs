import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const SignupPasswordSetup = () => {
  return (
    <View>
      <Text>SignupPasswordSetup</Text>
    </View>
  );
};

export default SignupPasswordSetup;

const styles = StyleSheet.create({});
